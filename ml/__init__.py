"""EthicLens ML workspace: dataset loaders, model training, the golden oracle, CLI.

This is a *flat* (non-distributable) package. Everything here imports the shared
``fairness_core`` engine so that experiments and the production service compute
fairness identically.
"""
