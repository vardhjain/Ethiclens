# Heavier ML/experiment dependencies for the `ml/` workspace (training, golden
# oracle, real-benchmark loaders, notebooks). The pure engine in
# packages/fairness-core has none of these.
joblib>=1.3
folktables>=0.0.12
matplotlib>=3.8
mlflow>=2.14
requests>=2.31
